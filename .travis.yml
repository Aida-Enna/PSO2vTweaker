os: linux
dist: trusty
sudo: required
group: stable
language: c
cache:
  directories:
   - $HOME/local/vitasdk

env:
  global:
    - VITASDK=$HOME/local/vitasdk
    - VDPMPATH=$HOME/local/vdpm


before_install:
  - git clone https://github.com/vitasdk/vdpm $VDPMPATH
  - cd $VDPMPATH && pwd
  - bash -x ./bootstrap-vitasdk.sh

install:
  - export PATH="$VITASDK:$PATH"
  - cd $VDPMPATH && pwd
  - ./install-all.sh

script:
  - export PATH="$VITASDK:$PATH"
  - cmake
